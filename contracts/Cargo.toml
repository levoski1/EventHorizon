[workspace]
resolver = "2"
members = [
    "airdrop",
    "boilerplate",
    "token_vesting",
    "staking",
    "escrow",
    "amm_pool",
    "dao_governance",
    "lending_base",
]

[profile.release]
opt-level = "z"
overflow-checks = true
debug = false
panic = "abort"
incremental = false
codegen-units = 1
lto = true
